{
  "$comment": "SINGLE SOURCE OF TRUTH for the tool catalogue. TS ops.ts is GENERATED from this (scripts/gen-ops.mjs); Rust tools.rs is VERIFIED against it (cargo test). Edit here, then regenerate + test.",
  "tools": [
    {
      "name": "tab_list",
      "uiLabel": "List all tabs",
      "risk": "low",
      "scope": "tab",
      "permission": "tabs",
      "confirmation": "none",
      "description": "List all open browser tabs. Returns id, title, url, and which is active.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "tab_focus",
      "uiLabel": "Switch to a tab",
      "risk": "low",
      "scope": "tab",
      "permission": "tabs",
      "confirmation": "none",
      "description": "Bring a tab to the foreground (make it active).",
      "inputSchema": {
        "properties": {
          "tabId": {
            "description": "Tab id from tab_list",
            "type": "integer"
          }
        },
        "required": [
          "tabId"
        ],
        "type": "object"
      }
    },
    {
      "name": "tab_open",
      "uiLabel": "Open a new tab",
      "risk": "medium",
      "scope": "tab",
      "permission": "tabs",
      "confirmation": "none",
      "description": "Open a URL in a new tab.",
      "inputSchema": {
        "properties": {
          "url": {
            "description": "Absolute URL to open",
            "type": "string"
          }
        },
        "required": [
          "url"
        ],
        "type": "object"
      }
    },
    {
      "name": "tab_close",
      "uiLabel": "Close a tab",
      "risk": "high",
      "scope": "tab",
      "permission": "tabs",
      "confirmation": "none",
      "description": "Close a browser tab by `tabId`.",
      "inputSchema": {
        "properties": {
          "tabId": {
            "description": "Tab id from tab_list",
            "type": "integer"
          }
        },
        "required": [
          "tabId"
        ],
        "type": "object"
      }
    },
    {
      "name": "page_snapshot",
      "uiLabel": "Snapshot interactive elements",
      "risk": "low",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Capture the active tab's interactive elements as an accessibility-style tree. Each node has a stable `ref` (e.g. \"e3\"), a role, an accessible name, and a fallback CSS selector. Use the `ref` in page_click/page_fill when possible. Same-origin sub-frames are included automatically; a sub-frame node's `ref` is prefixed `f<frameId>:` (e.g. \"f2:e3\") and page_click/page_fill route it back to that frame.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_click",
      "uiLabel": "Click an element",
      "risk": "high",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Click an element on the active tab. Prefer passing `ref` (from page_snapshot); fall back to `selector`.",
      "inputSchema": {
        "properties": {
          "ref": {
            "description": "Element ref from page_snapshot, e.g. \"e3\"",
            "type": "string"
          },
          "selector": {
            "description": "CSS selector fallback",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_fill",
      "uiLabel": "Fill a form field",
      "risk": "high",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Type a value into a form field on the active tab. Prefer `ref`; fall back to `selector`. Password fields are masked in logs/history.",
      "inputSchema": {
        "properties": {
          "ref": {
            "description": "Element ref from page_snapshot",
            "type": "string"
          },
          "selector": {
            "description": "CSS selector fallback",
            "type": "string"
          },
          "value": {
            "description": "Text to type into the field",
            "type": "string"
          }
        },
        "required": [
          "value"
        ],
        "type": "object"
      }
    },
    {
      "name": "page_text",
      "uiLabel": "Read page text",
      "risk": "medium",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Return the text content of the active tab (sensitive fields masked). `mode` \"visible\" (default) returns only rendered text — it excludes display:none / hidden inactive-tab panels; `mode` \"full\" also includes that hidden/inactive-tab text (script/style/noscript stripped). Use \"full\" when content is split across tabs/accordions. Same-origin sub-frames are appended under a frame marker.",
      "inputSchema": {
        "properties": {
          "mode": {
            "description": "\"visible\" (default, rendered text only) or \"full\" (include hidden / inactive-tab content)",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_links",
      "uiLabel": "Extract links (mailto/tel/href)",
      "risk": "medium",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Return the links on the active tab as an array of {text, href, type}, where type is one of mailto | tel | external | internal | anchor. Surfaces contact links (mailto:/tel:) and href targets that page_text only shows as anchor labels, and works even when page_snapshot is empty. hrefs are masked (token-like query strings redacted; emails / phone numbers preserved). Optional `type` filters to one kind; result is capped at 500. Includes links from same-origin sub-frames.",
      "inputSchema": {
        "properties": {
          "type": {
            "description": "Optional filter: one of mailto | tel | external | internal | anchor",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_screenshot",
      "uiLabel": "Capture the viewport",
      "risk": "medium",
      "scope": "page",
      "permission": "tabs",
      "confirmation": "none",
      "description": "Capture the visible viewport of the active tab as a PNG (base64).",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_scroll",
      "uiLabel": "Scroll the page",
      "risk": "low",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Scroll the active tab. Pass `direction` (up|down|top|bottom) or `pixels`.",
      "inputSchema": {
        "properties": {
          "direction": {
            "description": "One of: up, down, top, bottom",
            "type": "string"
          },
          "pixels": {
            "description": "Number of pixels to scroll (positive = down)",
            "type": "integer"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_wait_for",
      "uiLabel": "Wait for a condition",
      "risk": "low",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Wait until a condition is met on the active tab, or until timeout. One of: `selector` exists (optionally at least `minCount` matches), `text` appears, `nav` waits for the page to load (`until`: \"load\" default, or \"domcontentloaded\"), or `settled` waits for the DOM to stop mutating. SPA hash-route changes fire no navigation event — use `settled`, `selector`, or `text` for those.",
      "inputSchema": {
        "properties": {
          "nav": {
            "description": "Wait for a navigation event",
            "type": "boolean"
          },
          "until": {
            "description": "For `nav`: readiness level, \"load\" (default, full page load) or \"domcontentloaded\" (DOM parsed)",
            "type": "string"
          },
          "selector": {
            "description": "Wait for this selector to match an element",
            "type": "string"
          },
          "minCount": {
            "description": "With `selector`: wait until at least this many elements match (default 1)",
            "type": "integer"
          },
          "text": {
            "description": "Wait for this text to appear in the page",
            "type": "string"
          },
          "settled": {
            "description": "Wait until the DOM stops mutating for ~500ms (SPA/lazy-content friendly)",
            "type": "boolean"
          },
          "timeoutMs": {
            "description": "Max wait in ms (default 30000)",
            "type": "integer"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "page_eval",
      "uiLabel": "Execute arbitrary JS (high-risk)",
      "risk": "critical",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "HIGH RISK — execute arbitrary JavaScript on the active tab. The return value is masked (JWT / long hex / long numbers / token-like strings). This is the most powerful tool: prefer page_click / page_fill / page_snapshot whenever possible, and only use page_eval when those cannot achieve the goal (custom events, reading framework state, SPA routing, canvas/WebGL, etc.). Code runs in the page's global scope, wrapped as `async`, so you can `await` and `return` a value. Async results are awaited. Errors are returned as {name, message}. REQUIRES the extension's \"CDP mode\" setting: Chrome forbids the extension from evaluating code in the page on every site, so with CDP mode off this tool always fails — the error says so, and you should relay it to the user (they enable it in the extension's Options page) rather than retrying or working around it. Every other tool works without it.",
      "inputSchema": {
        "properties": {
          "code": {
            "description": "JavaScript code to execute",
            "type": "string"
          }
        },
        "required": [
          "code"
        ],
        "type": "object"
      }
    },
    {
      "name": "page_snapshot_precise",
      "uiLabel": "Precise snapshot (via debugger)",
      "risk": "medium",
      "scope": "page",
      "permission": "debugger",
      "confirmation": "warn",
      "description": "Like page_snapshot, but uses Chrome's debugger (CDP Accessibility.getFullAXTree) to capture the AUTHORITATIVE accessibility tree — accurate for shadow DOM and complex ARIA where the content-script approximation misses. The user is warned first (a brief on-page notice); Chrome then shows a 'Started debugging this browser' banner on all tabs for ~1 second while the snapshot is taken, then it disappears. Cannot run on chrome:// / web store pages, or tabs with DevTools open. Reads EVERY same-process frame, so iframe content is included; a cross-origin (out-of-process) frame cannot be read from a tab-level attach and is reported in a `note` instead of being silently dropped. Refs use a 'p' prefix (p1, p2...), are unique across frames, and work with page_click / page_fill unchanged wherever the element lives. Use this when page_snapshot misses elements or roles look wrong.",
      "inputSchema": {
        "properties": {
          "frameId": {
            "description": "Optional: limit to a specific frame's tree",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "cookie_get",
      "uiLabel": "Read cookies (redacted)",
      "risk": "high",
      "scope": "tab",
      "permission": "cookies",
      "confirmation": "none",
      "description": "Read cookies for the active tab (or a url/domain you specify). Includes httpOnly cookies (the main reason to use this over document.cookie). Read-only; there is no cookie_set (writing httpOnly cookies is a session-fixation risk). Values are masked before being returned — by pattern (JWT / long hex / long digit runs / provider API keys) and by name, so a cookie called e.g. `csrftoken` or `sessionid` is redacted whatever its value looks like. If you omit url/domain/name, cookies for the active tab's URL are returned.",
      "inputSchema": {
        "properties": {
          "domain": {
            "description": "Match this domain and its subdomains",
            "type": "string"
          },
          "name": {
            "description": "Exact cookie name to match",
            "type": "string"
          },
          "url": {
            "description": "Return cookies that would be sent to this URL",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    },
    {
      "name": "storage_get",
      "uiLabel": "Read localStorage/sessionStorage (redacted)",
      "risk": "high",
      "scope": "page",
      "permission": "scripting",
      "confirmation": "none",
      "description": "Read the page's localStorage or sessionStorage (where frameworks like Auth0 / NextAuth / Firebase store tokens). Must run on the active tab; same-origin only (cross-origin iframes are not readable). Pass `key` to fetch one entry, or omit it to dump all entries (capped at 500). Values are ALWAYS masked — by pattern (JWT / long hex / long digit runs / provider API keys) and by key name, so an entry called e.g. `apiKey` or `authToken` is redacted whatever its value looks like. This masking is not toggleable. Read-only.",
      "inputSchema": {
        "properties": {
          "key": {
            "description": "Specific key to read; omit for all entries",
            "type": "string"
          },
          "type": {
            "description": "\"local\" (default) or \"session\"",
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      }
    }
  ]
}

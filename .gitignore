# Rust build artifacts
/target/

# Node (tests/ uses puppeteer-core for the extension smoke test)
tests/node_modules/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local install destination (binaries live here after install.sh)
# ~/.browser-bridge/ is outside the repo, but guard against accidental copies
/browser-bridge
/browser-bridge.exe
*.dSYM/

# Test scratch
/tmp/

# ZCode working area (plans, session state)
/.zcode/

# Development worktrees (see CONTRIBUTING.md — one per branch, never committed)
/.worktree/

# Extension build output + deps
/extension/dist/
/extension/node_modules/

# Chrome Web Store upload package (built from dist/, never committed)
/browser-bridge-extension-cws-*.zip

# Playwright MCP scratch
/.playwright-mcp/

# Extension signing keys — never commit
*.pem
